<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta http-equiv="Content-Security-Policy" content="default-src 'self' https://cdnjs.cloudflare.com; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com;">
        
        <!-- Font Awesome -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

        {{#each styles}}
            <link rel="stylesheet" href={{this}} />
        {{/each}}

        {{#each scripts}}
            <script src={{this}} type="module"></script>
        {{/each}}

        <title>{{titre}}</title>

        <style>
            /* Style pour la navigation */
            nav {
                background-color: #1a1a1a;
                padding: 1rem;
                margin-bottom: 2rem;
            }

            nav .nav-container {
                max-width: 1200px;
                margin: 0 auto;
                display: flex;
                justify-content: space-between;
                align-items: center;
            }

            .logo {
                color: white;
                font-size: 1.5rem;
                font-weight: bold;
                text-decoration: none;
            }

            .nav-links {
                display: flex;
                gap: 2rem;
            }

            .nav-links a {
                color: white;
                text-decoration: none;
                padding: 0.5rem 1rem;
                border-radius: 4px;
                transition: background-color 0.3s;
            }

            .nav-links a:hover {
                background-color: #333;
            }

            /* Style pour le footer */
            footer {
                text-align: center;
                padding: 1rem;
                background-color: #f5f5f5;
                margin-top: 2rem;
            }
        </style>
    </head>
    <body>
        <nav>
            <div class="nav-container">
                <a href="/" class="logo">Planify</a>
                <div class="nav-links">
                    <a href="/">Accueil</a>
                    <a href="/board">Tableau des Tâches</a>
                    <a href="/contact">Contact</a>
                </div>
            </div>
        </nav>
        <main>
            {{{body}}}
        </main>
        <footer>&copy; Fatima ezzahra El kanouni</footer>
    </body>
</html>